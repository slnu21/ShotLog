<Window x:Class="ShotLog.Inbox.InboxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:ShotLog.Resources"
        Title="{x:Static res:Strings.Inbox_Title}" Height="660" Width="940" Icon="/Assets/shotlog.ico"
        Background="{StaticResource Bg}" WindowStartupLocation="CenterScreen">
  <Window.Resources>
    <Style x:Key="Card" TargetType="Border">
      <Setter Property="Background" Value="{StaticResource Surface}"/>
      <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="12"/>
      <Setter Property="Padding" Value="12"/>
      <Setter Property="Margin" Value="0,0,0,11"/>
      <Style.Triggers>
        <DataTrigger Binding="{Binding HasMemo}" Value="False">
          <Setter Property="BorderBrush" Value="#FF5C5326"/>
          <Setter Property="Background" Value="#FF1C1A12"/>
        </DataTrigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>

  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- toolbar -->
    <Grid Grid.Row="0" Margin="0,0,0,14">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBox x:Name="SearchBox" Grid.Column="0" Style="{StaticResource FieldBox}"
               TextChanged="OnSearchChanged"/>
      <Button Grid.Column="1" Content="{x:Static res:Strings.Inbox_Refresh}" Style="{StaticResource Btn}" Margin="10,0,0,0" Click="OnRefresh"/>
      <Button Grid.Column="2" Content="{x:Static res:Strings.Inbox_ComposeButton}" Style="{StaticResource BtnPrimary}" Margin="10,0,0,0" Click="OnCompose"/>
    </Grid>

    <!-- preset filter -->
    <WrapPanel x:Name="PresetFilter" Grid.Row="1" Margin="0,0,0,8"/>

    <!-- list -->
    <Grid Grid.Row="2">
      <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,8,0">
        <ItemsControl x:Name="List">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Style="{StaticResource Card}">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="148"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>

                  <!-- thumb -->
                  <Border Grid.Column="0" CornerRadius="9" BorderBrush="#FF2A3340" BorderThickness="1"
                          ClipToBounds="True" Height="90" Background="#FF0B0F15" Cursor="Hand"
                          MouseLeftButtonUp="OnOpenImage" Tag="{Binding}">
                    <Grid>
                      <Image Source="{Binding Thumb}" Stretch="UniformToFill"/>
                      <TextBlock Text="{x:Static res:Strings.Inbox_NoImage}" Foreground="{StaticResource TextDim}" FontSize="11"
                                 HorizontalAlignment="Center" VerticalAlignment="Center"
                                 Visibility="{Binding ThumbMissing, Converter={StaticResource BoolVis}}"/>
                    </Grid>
                  </Border>

                  <!-- memo + tags -->
                  <StackPanel Grid.Column="1" Margin="14,0,14,0">
                    <TextBox Text="{Binding Memo, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                             Style="{StaticResource FieldBox}" AcceptsReturn="True" TextWrapping="Wrap"
                             MinHeight="44" VerticalContentAlignment="Top"/>
                    <Grid Margin="0,8,0,0">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                      </Grid.ColumnDefinitions>
                      <TextBox Grid.Column="0" Text="{Binding TagsText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                               Style="{StaticResource FieldBox}" Padding="9,6"/>
                      <Border Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center"
                              Background="{StaticResource Surface2}" BorderBrush="{StaticResource Border}"
                              BorderThickness="1" CornerRadius="7" Padding="9,5">
                        <TextBlock Text="{Binding PresetName}" FontSize="11.5" Foreground="{StaticResource TextMuted}"/>
                      </Border>
                    </Grid>
                  </StackPanel>

                  <!-- right: time + delete -->
                  <StackPanel Grid.Column="2" VerticalAlignment="Top" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding TimeText}" FontSize="11.5" Foreground="{StaticResource TextDim}"
                               TextAlignment="Right" Margin="0,2,0,8"/>
                    <Button Content="{x:Static res:Strings.Inbox_DeleteButton}" Style="{StaticResource Btn}" Padding="10,6" FontSize="12"
                            HorizontalAlignment="Right" Click="OnDelete" Tag="{Binding}"/>
                  </StackPanel>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>

      <TextBlock x:Name="EmptyHint" Text="{x:Static res:Strings.Inbox_Empty}"
                 Foreground="{StaticResource TextDim}" HorizontalAlignment="Center" VerticalAlignment="Center"
                 Visibility="Collapsed"/>
    </Grid>
  </Grid>
</Window>
